The imagekit layer is supposed to provide functionality for uploading images to the ImageKit API (https://imagekit.io/docs)

Some already built modules are present under /composables and /utils. The code is written in JavaScript, transform that 
to TypeScript to nicely integrate into this project.
