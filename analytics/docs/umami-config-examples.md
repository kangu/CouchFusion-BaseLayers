# Umami Config Examples Documentation Update

## Initial Prompt
Extend the documentation to show how to configure the umami public website, ideally with a setting on the dev server and one in production.

## Implementation Summary
Manual documentation update; no code generator output was produced.

## Documentation Overview
- Expanded `layers/analytics/README.md` with concrete `.env` and `.env.production` examples that map Umami configuration to development and production environments.
- Clarified how Nuxt/bun reads environment variables during local and production runs.

## Implementation Examples
- Development environment snippet (`layers/analytics/README.md:49`).
- Production environment snippet (`layers/analytics/README.md:55`).
