# Array Reordering

- [x] Allow array reordering through drag and drop inside elements with `jsonarray` or `stringarray` types. Ensure the drag-and-drop operation works correctly locally and mutates the underlying source data.
- [x] Keep the top-level drag and drop of nodes functioning as before.
