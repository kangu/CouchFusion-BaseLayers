Implement a getSession function in the CouchDB module that queries /_session with a GET request and returns the result.
The input parameters can be: AuthSession cookie, base64 basic authorization token.
